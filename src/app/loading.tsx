import Loader from '@/components/shared/Loader/Loader';
import React from 'react';

const Loading = () => {
    return (
        <div>
            <Loader/>
        </div>
    );
};

export default Loading;